My Emacs Configuration on Windows
=================================

Rewrite from scratch instead of just copying from the version on Linux.

Changes:

- Use `use-package`
- Use emacs's new package manager instead of cask